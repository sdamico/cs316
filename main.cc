#include "core/register.h"

int main(int argc, char **argv) {
  // TODO(sam): Take input via args (pick file to use, etc)
  // TODO(sam): Process input file, preload RAM, etc
  // TODO(sam): Invoke main hardware class Run() method (or something)
  // TODO(sam): Write output file
}
